def create_tiles(aerial_path, tile_size_m, overlap_percentage, pixels_to_meters):
    # 1. Convert tile size in meters to tile size in pixels
    # 2. Calculate the amount of horizontal / vertical tiles
    